streamlit==1.32.0
opencv-python-headless==4.9.0.80  # Headless version (no GUI dependencies)
numpy==1.26.0  # Often required with OpenCV
Pillow==10.1.0  # Useful for image processing
pytesseract==0.3.10
Pillow==10.1.0  # Required for image processing
# Add other Python packages you use below
